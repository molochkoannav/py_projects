[project]
name = "py-projects"
version = "0.1.0"
description = ""
authors = [
    {name = "Ann Molochko",email = "molochkoannav@gmail.com"}
]
requires-python = ">=3.14"
dependencies = [
    "pytest (>=9.1.0,<10.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
lint = [
    "flake8 (>=7.3.0,<8.0.0)",
    "black (>=26.5.1,<27.0.0)",
    "isort (>=8.0.1,<9.0.0)",
    "mypy (>=2.1.0,<3.0.0)"
]
dev = [
    "setuptools (>=82.0.1,<83.0.0)",
    "pytest-cov (>=7.1.0,<8.0.0)"
]
